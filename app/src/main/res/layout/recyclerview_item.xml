<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.cardview.widget.CardView
        android:id="@+id/item_cardview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/item_image"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:src="@drawable/soori"/>
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/item_site_url"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="content title"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/item_cardview"
        app:layout_constraintStart_toStartOf="@id/item_cardview"/>

    <TextView
        android:id="@+id/item_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="YYYY-MM-DD hh:mm:ss"
        android:layout_marginTop="3dp"
        app:layout_constraintTop_toBottomOf="@id/item_site_url"
        app:layout_constraintStart_toStartOf="@id/item_site_url"/>


</androidx.constraintlayout.widget.ConstraintLayout>